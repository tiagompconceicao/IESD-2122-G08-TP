syntax = "proto3";
import "ITransactionManagerTX.proto";
option java_multiple_files = true;
option java_package = "transactionManagerAX";

package forum;

service ITransactionManagerAX {
  rpc prepare(Transaction) returns (Result);
  rpc commit (Transaction) returns (Result);
  rpc rollback (Transaction) returns (Result);
}

